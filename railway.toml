[build]
builder = "dockerfile"
dockerfilePath = "Dockerfile.web"

[deploy]
startCommand = "python manage.py migrate --noinput && python manage.py collectstatic --noinput && python manage.py init_superuser && gunicorn querygrade.wsgi --log-file - --log-level info --bind 0.0.0.0:$PORT"
