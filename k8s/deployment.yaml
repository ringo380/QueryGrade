apiVersion: apps/v1
kind: Deployment
metadata:
  name: querygrade
spec:
  replicas: 3
  selector:
    matchLabels:
      app: querygrade
  template:
    metadata:
      labels:
        app: querygrade
    spec:
      containers:
      - name: querygrade
        image: yourusername/querygrade:latest
        ports:
        - containerPort: 8000
        env:
        - name: DEBUG
          value: "True"
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: prometheus
spec:
  replicas: 1
  selector:
    matchLabels:
      app: prometheus
  template:
    metadata:
      labels:
        app: prometheus
    spec:
      containers:
      - name: prometheus
        image: prom/prometheus
        ports:
        - containerPort: 9090
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: grafana
spec:
  replicas: 1
  selector:
    matchLabels:
      app: grafana
  template:
    metadata:
      labels:
        app: grafana
    spec:
      containers:
      - name: grafana
        image: grafana/grafana
        ports:
        - containerPort: 3000
